import React from "react";
import { ButtonHeader, HeaderComponent } from "./StylesHeader";

const Header = () => {
  return (
    <HeaderComponent>
      <ButtonHeader></ButtonHeader>
    </HeaderComponent>
  );
};

export default Header;
